<%= I18n.t('spree.order_mailer.cancel_email.dear_customer') %>

<%= I18n.t('spree.order_mailer.cancel_email.instructions') %>

============================================================
<%= I18n.t('spree.order_mailer.cancel_email.order_summary_canceled') %>
============================================================
<% @order.line_items.each do |item| %>
  <%= item.variant.sku %> <%= raw(item.variant.product.name) %> <%= raw(item.customized? ? item.options_text : item.variant.options_text) -%> (<%=item.quantity%>) @ <%= item.single_money %> = <%= item.display_amount %>
<% end %>
============================================================
<%= I18n.t('spree.order_mailer.cancel_email.subtotal') %> <%= @order.display_item_total %>
<% @order.adjustments.eligible.each do |adjustment| %>
  <%= raw(adjustment.label) %> <%= adjustment.display_amount %>
<% end %>
<%= I18n.t('spree.order_mailer.cancel_email.total') %> <%= @order.display_total %>
